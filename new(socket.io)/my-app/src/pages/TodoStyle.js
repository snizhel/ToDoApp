export default (theme) => ({
    container: {
        backgroundColor: "#f0f0f0",
        padding: "(8,0,6)"
    },
    submit: {
        visibility: "hidden",
    },
    header: {
        marginTop: "2%",
        marginBottom: "2%"
    },
    test: {
        backgroundColor: "red"
    }
});